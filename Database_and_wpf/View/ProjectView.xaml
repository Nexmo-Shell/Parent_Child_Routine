<UserControl x:Class="Database_and_wpf.View.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Database_and_wpf.View"
             xmlns:vm="clr-namespace:Database_and_wpf.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">
    <UserControl.DataContext>
        <vm:ProjectViewModel/>
    </UserControl.DataContext>
    <Border CornerRadius="30" Background="WhiteSmoke" DataContext="{Binding Project}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock HorizontalAlignment="Left" VerticalAlignment="Top" Text="{Binding ID_Project}" Margin="20,5,5,5" FontSize="14" FontWeight="Bold"/>
            <Grid Grid.Row="1">
                <WrapPanel Orientation="Horizontal" Margin="10">
                    <TextBlock Margin="5"  HorizontalAlignment="Center" VerticalAlignment="Center" Text="This is a new Project. Add your Boards here." FontSize="16" />
                </WrapPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
